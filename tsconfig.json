{
  // Solution file: one program per side (host half vs browser half) — the
  // host context merges (webServer/systemPrompt) and the browser context
  // merges (slots/locale/runtime) cannot share one ts.Program (TS2717),
  // the same rule the dsh-web-ui family packages follow.
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
